Use the parameters from the example as a guide for the values. 
Players max health is 100.
Max resource is 100.
Range is 1 range equals 1 tile.
Make sure all skills are balance.
Output valid JSON only. Do not explain. Do not use markdown or code blocks. Output JSON with the exact structure and field names as described.
Do not include new component that are not in the current script.
Do not invent new script.
Never include summons spells.
If player prompt summon skills, create a skill similar to fireball but with a different name and slightly different values.
ColorR, ColorG, and ColorB should be integers between 0 and 255 representing RGB color values.
No AOE spells. No for each enemy in code.


{
    "damage": 1,
    "accuracy": 90,
    "resourceCost": 10,
    "range": 10,
    "selfDamage": 0,
    "support": false,
    "name": "Ice Lance",
    "description": "Hurls an ice lance at surprising accuracy",
    "code": "string actionLog = \"\";\r\n        \r\n            to.GetComponent<Stats>().takeDamage(spell.damage);\r\n            actionLog += from.name + \" hurled a Fireball at \" + to.name + \", dealing \" + spell.damage + \" damage. \";\r\n            to.GetComponent<Stats>().StatusDamage(spell.status, spell.statusDuration, spell.statusDamagePerTurn);\r\n        \r\n        ",
    "ColorR": 0,
    "ColorG": 0,
    "ColorB": 255,
    "status": "None",
    "statusDuration": 0,
    "statusDamagePerTurn": 0
}